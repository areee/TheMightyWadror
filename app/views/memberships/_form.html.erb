<%= form_for(@membership) do |f| %>
  <% if @membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

      <ul>
      <% @membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.select :beerclub_id, options_from_collection_for_select(@beerclubs, :id, :to_s) %>
    <!--user: <%= f.select :user_id, options_from_collection_for_select(@users, :id, :to_s) %>-->
    user: <%= f.select :user_id, options_for_select([[current_user,current_user.id]]) %>
    <%= f.submit %>
  </div>
<% end %>
